<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fzn.classsign">
    <!-- 申请必要的权限 -->
    <!-- 用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- 用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- 用于获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /> <!-- 用于访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- 用于写入缓存数据到扩展存储卡 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 用于申请调用A-GPS模块 -->
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResetPasswordActivity"></activity>
        <activity android:name=".RecoverPasswordActivity" />
        <activity android:name=".SetPasswordActivity" />
        <activity android:name=".GetVerificationCodeActivity" />
        <activity android:name=".PasswordLoginActivity" />
        <activity android:name=".QuickLoginActivity" />
        <activity android:name=".LoginSelectionActivity" />
        <activity android:name=".UpdateInfoActivity" />
        <activity android:name=".UpdatePasswordActivity" />
        <activity android:name=".MyInfoActivity" />
        <activity android:name=".SignInRecordListActivity" />
        <activity android:name=".MemberListActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".CreateSignInActivity" />
        <activity android:name=".ClassDetailActivity" />
        <activity android:name=".JoinClassActivity" />
        <activity android:name=".CreateClassActivity" />
        <activity android:name=".ClassHomePageActivity" />
        <activity android:name=".ScanActivity" />
        <activity android:name=".SignInResultActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 设置key -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="ClassSignKey" /> <!-- 声明定位service -->
        <service android:name="com.amap.api.location.APSService" />
    </application>

</manifest>